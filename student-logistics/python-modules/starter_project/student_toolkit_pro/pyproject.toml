[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "student-toolkit-pro"
version = "0.1.0"
description = "A modular Python CLI project for learning modules and packages"
requires-python = ">=3.12"
dependencies = [
    "rich",
]
